# Project Spector - core runtime
#
# Pure-Python wheels only: installs with no compiler and no model downloads.
# These are all you need to run the full event pipeline, memory, and voice
# layers in mock mode, and to run the test suite.
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pyyaml==6.0.1
python-multipart==0.0.6
python-dotenv==1.0.0
requests==2.31.0
tqdm==4.66.1
httpx==0.25.2          # used by the FastAPI test client in the test suite

# Optional: real AI models (local LLM inference, Whisper STT, GPU acceleration)
# pull in large / compiled dependencies and are NOT required for mock mode.
# Install them only when you want real inference instead of mock fallbacks:
#
#     pip install -r requirements-ml.txt
